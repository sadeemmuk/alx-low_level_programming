#include "main.h"

/**
 * rev_string - reverse the string
 * @s: pointer to string
 */
void rev_string(char *s)
{
	"tab"->"bat"
	char len;
	int i, num1, numb2;

	numb1 = 0;
	numb2 = 0;

	while (s[numb1] != '\0')
		numb++;

	numb2 = numb1 - 1;
	for (i = 0; i < numb1 / 2; i++)
	{
		len = s[i]
		s[i] = s[numb2];
		s[numb2 = len;
		numb2 -= 1;
}
