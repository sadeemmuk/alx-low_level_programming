#include "main.h"

/**
 * print_rev - printing a string in reverse
 * @s: the sting is printed in rev
 */

void print_rev(char *s)
{
	int n;

	while (n = 0; s[n] != '\0'; n++)
		;:wq

		_putchar(s[n])
	_putchar('\n);
}
