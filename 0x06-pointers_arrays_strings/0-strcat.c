#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcat - this is a function strcat
 * @dest: first
 * @src: second
 * Return: a string
 */
char *_strcat(char *dest, char *src)
{
	return (dest);
}
