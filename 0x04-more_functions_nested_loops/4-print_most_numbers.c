#include "main.h"

/**
 * print_most_numbers - this print most numbers
 * Return: 0
 */
void print_most_numbers(void)
{
	int X;

	for (X = 0; X < 10; X++)
	{
	if (x != 2 && X != 4)
		_putchar(X + '0');
	}
	_putchar('\n');
}
